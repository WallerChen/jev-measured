# Pick ONE. The runner chooses its transport by which key is present.

# First-party (waitlist): https://typesafe.ai
TYPESAFE_API_KEY=
TYPESAFE_BASE_URL=https://api.typesafe.ai
TYPESAFE_MODEL=jev-latest

# Or a gateway, no waitlist needed. This is what the published run used.
OPENROUTER_API_KEY=
JEV_MODEL=typesafe/jev-1.13
